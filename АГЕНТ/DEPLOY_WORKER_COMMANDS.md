# 🚀 Команды для деплоя Cloudflare Worker (через терминал)

Раз интерфейс ругается, сделаем всё профессионально через консоль. Это самый надежный способ.

### 1. Установка инструмента (один раз)
Выполни эту команду в папке `АГЕНТ`:
```powershell
npm install -D wrangler
```

### 2. Вход в аккаунт
Тебя перекинет в браузер, чтобы подтвердить вход.
```powershell
npx wrangler login
```
*Нажми "Allow" в браузере.*

### 3. Деплой (Загрузка кода)
Эта команда отправит твой код на сервера Cloudflare.
```powershell
npx wrangler deploy
```
*Если спросит, создать ли проект - отвечай Y (Yes).*

### 4. Добавление Секретного Ключа
Самый важный шаг. Вставь эту команду, нажми Enter, и потом **вставь свой ключ** (он не будет виден на экране при вводе).
```powershell
npx wrangler secret put OPENROUTER_API_KEY
```
*(Вставь ключ `sk-or-v1-...` и нажми Enter)*

---

### 🎉 Готово!
В консоли после деплоя ты увидишь ссылку, например:
`https://agent-proxy.твой-ник.workers.dev`

Скопируй её, она нам нужна для приложения.
