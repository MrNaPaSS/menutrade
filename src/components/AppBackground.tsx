import { MatrixRain } from '@/components/MatrixRain';
import { GraffitiBackdrop } from '@/components/graffiti/Graffiti';

/**
 * Фон приложения.
 *
 * Один компонент на все экраны: падающие символы плюс верхнее поле с
 * лендинга, обрывающееся рваной кромкой. Раньше эти два слоя ставились
 * рядом вручную в девяти файлах - про второй легко было забыть на новом
 * экране, и он выглядел как другое приложение.
 *
 * Стоит первым внутри экрана, до содержимого: оба слоя лежат на z-0, а
 * содержимое поднимается на z-10.
 */
export function AppBackground() {
    return (
        <>
            <MatrixRain />
            <GraffitiBackdrop />
        </>
    );
}
